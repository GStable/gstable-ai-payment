#!/usr/bin/env python3
"""
GStable AI Payment Skill - Unified Entry Point

Usage:
    # Run directly
    ./gstable-ai-payment get-link lnk_xxx
    ./gstable-ai-payment create-session lnk_xxx --chain 137 --token USDC
    ./gstable-ai-payment prepare sess_xxx --chain 137 --token 0x3c499c...
    ./gstable-ai-payment wallet

    # Or run via uv
    uv run gstable-ai-payment get-link lnk_xxx
"""

import sys
import os

# Ensure the src directory is in the Python path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))

from gstable_ai_payment.cli import cli

if __name__ == "__main__":
    cli()
